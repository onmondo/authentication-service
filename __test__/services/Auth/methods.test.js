// const chai = require('chai');
// const {expect} =  chai;

// const {getKey} = require('../../../services/Auth/methods');
// const dbClient = require('../../../utils/dbConnection');

// describe('test methods.js', () => {
//   context('test get key for mobile: 09296912977', () => {
//     it('should return no result', () => {
//       return new Promise(async(resolve, _reject) => {
//         const result = await getKey(dbClient, {key: '09296912977'});
//         expect(result).to.be.null;
//         resolve();
//       });
//     });
//   });
// });